#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
  int NUM_SIZE;
  if(argc == 2)
    {
      if(argv[1] != "-l")
	{
	  cout << "Invalid command" << endl;
	}
      else
	{
	  //set ai as first player
	}
    }

  if(argc == 3)
    {
      
    }
  
  if(argc == 4)
    {
      if(argv[1] == "-l")
	{
	  //set ai as first player
	  NUM_SIZE = argv[3];
	}
    }

}
